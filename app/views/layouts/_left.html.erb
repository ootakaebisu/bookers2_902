<h2>User info</h2>

<table>
  <tr>
    <th>name</th>
    <td><%= user.name %></td>
  </tr>

  <tr>
    <th>introduction</th>
    <td><%= user.introduction %></td>
  </tr>
</table>

<% if user == current_user %>
  <%= link_to "(仮)", edit_user_path(user.id) %>
<% end %>

<h2>New Book</h2>
<%= form_with model: @new_book,  locol: true do |f| %>
  <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <%= f.label :opinion %><br>
    <%= f.text_area :opinion %>
  </div>
  <div class="form-group">
    <%= f.submit "Create Book" %>
  </div>
<% end %>
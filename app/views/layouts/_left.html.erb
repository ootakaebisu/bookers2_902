<h2>User info</h2>

<table class="table">
  <tr>
    <%= attachment_image_tag user, :image, format: 'jpeg', size: "100x100" %>
  </tr>
  <tr>
    <th>name</th>
    <td><%= user.name %></td>
  </tr>

  <tr>
    <th>introduction</th>
    <td><%= user.introduction %></td>
  </tr>
</table>

<% if user == current_user %>
  <%= link_to "(仮)", edit_user_path(user.id), class: "btn btn-outline-secondary btn-block" %>
<% end %>

<h2 class="mt-3">New Book</h2>
<%= form_with model: @new_book,  locol: true do |f| %>
  <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: "form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :opinion %><br>
    <%= f.text_area :opinion, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit "Create Book", class: "btn btn-success" %>
  </div>
<% end %>